function giamgia() {
    val = parseInt(document.getElementsByName("input")[0].value);
    if (val == 31151020673) { alert(" Bạn được giảm 5% ");  }
    else { alert(" Mã giảm giá không hợp lệ "); }
    
}